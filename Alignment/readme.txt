
This folder is based on the codebase of Alignprop: "Aligning Text-to-Image Diffusion Models with Reward Backpropagation".

The folder scripts contains bash scripts to reproduce experiments on  Finetuning diffusion models to align with multiple rewards.

```mps_contrast_sat.sh``` with MPS, saturation, sharpness using our constrained approach and the fix weights baseline.
